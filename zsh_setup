1. sudo apt install zsh

2. chsh -s /bin/zsh (if this does not work, open terminal, go to Edit -> Profile preferences -> Run custom command -> zsh)

3. wget https://github.com/robbyrussell/oh-my-zsh/raw/master/tools/install.sh -O - | zsh (install oh-my-zsh)

